---
interface Props {
  title: string;
  description?: string;
  keywords?: string;
  ogImage?: string;
}

const { 
  title, 
  description = "Бани и веники", 
  keywords = "бани, сауны, парные, банный комплекс", 
  ogImage = "/default-og.png" 
} = Astro.props;
---

<!doctype html>
<html lang="ru">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="keywords" content={keywords} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta property="og:image" content={ogImage} />
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>

